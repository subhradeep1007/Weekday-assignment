import React from "react";
import { Wrapper } from "./styles";

const ZerothState = () => {
  return <Wrapper>No Jobs available for this category at the moment</Wrapper>;
};

export default ZerothState;
